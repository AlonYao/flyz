<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- selected -->
    <item android:state_selected="true" android:drawable="@drawable/offline_selected"/>

    <!-- selected -->
    <item android:state_checked="true" android:drawable="@drawable/offline_selected"/>

    <!-- not selected -->
    <item android:drawable="@drawable/offline_unselected"/>
</selector>