<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- selected -->
    <item android:state_selected="true" android:background="@color/practice_report_all"/>

    <!-- focused -->
    <item android:state_focused="true" android:background="@color/practice_report_all"/>

    <!-- pressed -->
    <item android:state_pressed="true" android:background="@color/practice_report_all"/>

    <!-- not selected -->
    <item android:background="@android:color/white"/>
</selector>